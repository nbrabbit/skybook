<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.rabbit.dao.SBookRecordDao">
    <sql id="BASE_TABLE">
        t_skybookrecord
    </sql>

    <sql id="BASE_COLUMN">
        id, skybookid, userip, invoketime
    </sql>
    <insert id="insertRecord" parameterType="com.rabbit.model.SBookRecord">
        insert into
            t_skybookrecord(<include refid="BASE_COLUMN" />)
        values ('001','111','222','333','444')
--         values (#{id},#{skybookid},#{userip},#{invoketime})
    </insert>
</mapper>